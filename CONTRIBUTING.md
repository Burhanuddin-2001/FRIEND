# Contributing to Friend

1.  **Fork and Clone**: Clone the repo locally.
2.  **Install**: Run `poetry install`.
3.  **Branch**: Create a branch for your feature.
4.  **Test**: Run `poetry run pytest` before pushing.
5.  **Lint**: Run `poetry run black .` and `poetry run isort .` to format code.
